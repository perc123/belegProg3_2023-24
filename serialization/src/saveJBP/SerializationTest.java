package saveJBP;

import administration.VendingMachine;

public class SerializationTest {

/*    public static void main(String[] args) {
        testSerialization();
        testDeserialization();
    }

    private static void testSerialization() {
        VendingMachine vendingMachine = createSampleVendingMachine(); // Implement this method to create a sample vending machine
        JBP jbp = new JBP(vendingMachine);

        // Serialize
        jbp.serialisierenJBP();
        System.out.println("Serialization successful");
    }

    private static void testDeserialization() {
        JBP jbp = new JBP(null); // Pass null for vending machine since it will be loaded
        VendingMachine loadedVendingMachine = jbp.deserialisierenJBP();

        if (loadedVendingMachine != null) {
            System.out.println("Deserialization successful");
        } else {
            System.out.println("Error in deserialization");
        }
    }

    private static VendingMachine createSampleVendingMachine() {
        // Implement this method to create a sample vending machine with some data
        // You can add items to the vending machine for testing
        VendingMachine vendingMachine = new VendingMachine(10);
        return vendingMachine;
    }*/
}
