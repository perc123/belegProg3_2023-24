package commands;

/*public enum CommandType {
    INSERT_MODE(":c", "Switch to insert mode"),
    DELETE_MODE(":d", "Switch to delete mode"),
    DISPLAY_MODE(":r", "Switch to display mode"),
    UPDATE_MODE(":u", "Switch to update mode"),
    PERSISTENCE_MODE(":p", "Switch to persistence mode"),
    SAVE_JOS("saveJOS", "Save data in JOS format"),
    LOAD_JOS("loadJOS", "Load data from JOS format"),
    SAVE_JBP("saveJBP", "Save data in JBP format"),
    LOAD_JBP("loadJBP", "Load data from JBP format"),
    EXIT("exit", "Exit the application"),
    ERROR("error", "Invalid command");

    private final String value;
    private final String description;

    CommandType(String value, String description) {
        this.value = value;
        this.description = description;
    }

    public String getValue() {
        return value;
    }

    public String getDescription() {
        return description;
    }
}*/
